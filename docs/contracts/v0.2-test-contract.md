# v0.2 process-behaviour test contract

This is the v0.2 counterpart to the v0.1 ten-proof test contract. v0.1 proved
the runtime's failure semantics with a fixed set of Erlang tools and a hardcoded
registry seed. v0.2 adds tool manifests, a descriptor registry, and a one-shot
`cli`/port adapter — and this doc states the v0.2 process-behaviour proof set in
one place and names the suite and case that proves each line.

Every CLI proof runs through the real `soma_agent_session` → `soma_run` →
`soma_tool_call` process layers, the same path the Erlang tools use. The
contract extends v0.1 without weakening it: a `cli` tool is just another tool
behind the same process boundary.

## How to read this map

Each proof below names:

- **Proof** — the process-behaviour guarantee being asserted.
- **Suite** — the test suite (EUnit module or Common Test suite) it lives in.
- **Case** — the test function that proves it.
- **Entry** — the call entry the test enters through, so you can confirm no
  layer is bypassed.

Follow a row to its suite and case and you can read the assertion that proves
the line. That is what makes the contract verifiable by following the map.

## The proof set

### Proof 1 — a manifest missing a required field is rejected, and that tool name does not resolve through the registry

The proof has two halves: a missing-field manifest is rejected, *and* that tool
name does not resolve through the running registry.

| Half | Suite | Case | Entry |
| --- | --- | --- | --- |
| Reject | `apps/soma_tools/test/soma_tool_manifest_tests.erl` | `test_normalize_rejects_missing_shared_field` | `soma_tool_manifest:normalize/1` (pure function) |
| Does not resolve | `apps/soma_tools/test/soma_tool_registry_tests.erl` | `test_register_tool_rejects_missing_field_name_unresolvable` | `soma_tool_registry:register_tool/1` on the running registry, then `soma_tool_registry:resolve_descriptor/1` returns `{error, not_found}` |

The does-not-resolve half is the one gap this issue closes with a new EUnit
case: it registers a missing-field manifest, asserts `register_tool/1` returns
the rejection, and asserts `resolve_descriptor/1` for that name returns
`{error, not_found}` — proving the registry is left unchanged.

### Proof 2 — built-ins register through manifest validation, and an `echo` run still completes end-to-end

| Half | Suite | Case | Entry |
| --- | --- | --- | --- |
| Register through manifest | `apps/soma_runtime/test/soma_run_happy_path_SUITE.erl` | `test_registry_seeds_descriptors_from_manifests` | `soma_tool_registry:resolve_descriptor/1` on the seeded registry, compared against `soma_tool_manifest:normalize(Module:manifest())` for each built-in |
| `echo` end-to-end | `apps/soma_runtime/test/soma_run_happy_path_SUITE.erl` | `test_multi_step_runs_sequentially_to_completed` | `soma_agent_session:start_run/2` (no layer bypassed) |

### Proof 3 — a `cli` tool drives a run to `completed`

| Suite | Case | Entry |
| --- | --- | --- |
| `apps/soma_runtime/test/soma_cli_adapter_SUITE.erl` | `test_cli_run_reaches_completed` | `soma_agent_session:start_run/2` → `soma_run` → `soma_tool_call` spawns the cli helper through a port → `run.completed` |

### Proof 4 — the `cli` call runs in its own worker, pid distinct from the run

| Suite | Case | Entry |
| --- | --- | --- |
| `apps/soma_runtime/test/soma_cli_adapter_SUITE.erl` | `test_cli_tool_call_has_distinct_pid` | `soma_agent_session:start_run/2`; the test reads the worker pid (carried on `tool.started`/`tool.succeeded`) and the run pid (`run_pid/0`) off the event trail and asserts they differ |

### Proof 5 — a successful `cli` call emits the same event trail as an Erlang tool

The trail is `tool.started`, `tool.succeeded`, `step.succeeded`, then
`run.completed`. No single case asserts all four in one ordered chain; two cases
together cover the trail. The contract states this split plainly rather than
papering over it.

| Half | Suite | Case | Entry |
| --- | --- | --- | --- |
| `tool.started < tool.succeeded < step.succeeded` | `apps/soma_runtime/test/soma_cli_adapter_SUITE.erl` | `test_cli_step_event_order` | `soma_agent_session:start_run/2` |
| `run.completed` in the same trail | `apps/soma_runtime/test/soma_cli_adapter_SUITE.erl` | `test_cli_run_reaches_completed` | `soma_agent_session:start_run/2` (same one-step cli run) |

### Proof 6 — nonzero exit and a missing/unrunnable executable each reach `failed`, session stays alive

| Half | Suite | Case | Entry |
| --- | --- | --- | --- |
| Nonzero exit reaches `failed` | `apps/soma_runtime/test/soma_cli_failure_SUITE.erl` | `test_non_zero_exit_carries_status` | `soma_agent_session:start_run/2` |
| Missing executable — named error | `apps/soma_runtime/test/soma_cli_failure_SUITE.erl` | `test_missing_executable_named_error` | `soma_agent_session:start_run/2` |
| Missing executable — reaches `run.failed` trail | `apps/soma_runtime/test/soma_cli_failure_SUITE.erl` | `test_missing_executable_reaches_run_failed_trail` | `soma_agent_session:start_run/2` |
| File exists but not executable | `apps/soma_runtime/test/soma_cli_failure_SUITE.erl` | `test_non_executable_permission_error` | `soma_agent_session:start_run/2` |
| Session stays alive across a cli failure | `apps/soma_runtime/test/soma_cli_failure_SUITE.erl` | `test_session_alive_runs_new_run_after_cli_failure` | `soma_agent_session:start_run/2` twice; asserts `is_process_alive(SessionPid)` between runs |

### Proof 7 — a hanging `cli` tool times out and its external OS process is gone

| Half | Suite | Case | Entry |
| --- | --- | --- | --- |
| Reaches `timeout` | `apps/soma_runtime/test/soma_cli_lifecycle_SUITE.erl` | `test_cli_overrun_reaches_timeout` | `soma_agent_session:start_run/2`; the per-step `state_timeout` fires and `soma_run` kills the worker |
| External process dead | `apps/soma_runtime/test/soma_cli_lifecycle_SUITE.erl` | `test_cli_external_process_dead_after_timeout` | proven by the absence of a marker file the helper would only touch if it had survived to finish its sleep |

### Proof 8 — cancelling a run with an active `cli` tool stops the external process and reaches `cancelled`

| Half | Suite | Case | Entry |
| --- | --- | --- | --- |
| Reaches `cancelled` | `apps/soma_runtime/test/soma_cli_lifecycle_SUITE.erl` | `test_cli_cancel_reaches_cancelled` | `soma_agent_session:start_run/2`, then the `{cancel_run, RunId}` message to the session |
| External process dead | `apps/soma_runtime/test/soma_cli_lifecycle_SUITE.erl` | `test_cli_external_process_dead_after_cancel` | proven by the absence of a marker file the helper would only touch if it had survived its sleep — the same marker-file liveness check as the timeout case |

### Proof 9 — after `failed`/`timeout`/`cancelled`, the same session completes a fresh run

| After | Suite | Case | Entry |
| --- | --- | --- | --- |
| Timeout | `apps/soma_runtime/test/soma_cli_lifecycle_SUITE.erl` | `test_session_alive_runs_new_cli_run_after_timeout` | `soma_agent_session:start_run/2` twice on one session pid |
| Cancel | `apps/soma_runtime/test/soma_cli_lifecycle_SUITE.erl` | `test_session_alive_runs_new_cli_run_after_cancel` | `soma_agent_session:start_run/2` twice on one session pid |
| Failure | `apps/soma_runtime/test/soma_cli_failure_SUITE.erl` | `test_session_alive_runs_new_run_after_cli_failure` | `soma_agent_session:start_run/2` twice on one session pid |

## Coverage and the build gate

Every proof above resolves to a real passing case. The one gap — the
does-not-resolve half of proof 1 — is closed by the new
`test_register_tool_rejects_missing_field_name_unresolvable` case in
`soma_tool_registry_tests`. No other proof is unmapped.

The merge gate runs `rebar3 eunit && rebar3 ct`. Common Test covers the cli
runtime/process guarantees (the cli suites); EUnit covers manifest validation
(`soma_tool_manifest_tests`) and registry behaviour (`soma_tool_registry_tests`).

## Drift caveat

This is a map, not new behaviour, so it can drift: rename a case or delete an
assertion and the doc points at something that no longer proves what it claims.
Every name here is copied from a case that runs in the gate, so a deleted case
shows up as a gate failure, even if not as a doc-drift failure.
