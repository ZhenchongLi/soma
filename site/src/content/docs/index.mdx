---
title: Soma
description: An Erlang/OTP-native agent runtime.
template: splash
hero:
  tagline: An Erlang/OTP-native agent runtime — an agent run is a supervised process tree, not a function calling tools in a loop.
  actions:
    - text: Get started
      link: /start/overview/
      icon: right-arrow
---

Soma treats every session, run, and tool call as a supervised OTP process.
Erlang/OTP provides the execution semantics — timeouts, cancellation,
monitoring, crash isolation, restart policy; the step list only says *what* to
run.
