---
// 简体中文 landing root, built to /zh/index.html.
// Standalone Astro page; <html lang="zh-CN"> and the language toggle anchor are
// authored by hand. Imports the same custom.css for the accent token and fonts.
import { Code } from 'astro:components';
import '../../styles/custom.css';

const quickStart = `# 启动守护进程，运行一个步骤列表
soma daemon
soma run ./pipeline.lfe
soma status`;
---

<html lang="zh-CN">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="icon" href="/favicon.svg" type="image/svg+xml" />
    <title>Soma — 一个 Erlang/OTP 原生的 agent 运行时</title>
    <meta
      name="description"
      content="一个 Erlang/OTP 原生的 agent 运行时：一次 agent 运行是一棵受监督的 OTP 进程树，而不是在循环里调用工具的函数。"
    />
  </head>
  <body>
    <nav class="landing-nav">
      <a class="brand" href="/zh/">Soma</a>
      <div class="nav-links">
        <a href="/zh/start/overview/">文档</a>
        <a href="https://github.com/ZhenchongLi/soma">GitHub</a>
        <a class="lang-toggle" href="/">English</a>
      </div>
    </nav>

    <header class="hero">
      <h1>一次 agent 运行是一棵受监督的 OTP 进程树。</h1>
      <p class="hero-tagline">
        Soma 是一个 Erlang/OTP 原生的 agent 运行时。每一个 session、run 和 tool
        call 都是一个受监督的 OTP 进程——而不是在循环里调用工具的函数。
      </p>
      <div class="hero-actions">
        <a class="cta primary" href="/zh/start/overview/">开始</a>
        <a class="cta" href="https://github.com/ZhenchongLi/soma">在 GitHub 上查看</a>
      </div>
    </header>

    <section class="thesis">
      <p>
        核心论点很简单：agent 会以运维的方式出错——它们会超时、会卡住、会崩溃、会被取消。
        OTP 本来就为这些场景提供了监督、监控和崩溃隔离语义，所以 Soma 把一次运行建模成
        一棵进程树，而不是一个进程内的循环。
      </p>
    </section>

    <section class="features">
      <article class="card">
        <h3>LFE DSL</h3>
        <p>
          一套受约束的 Lisp 风格语法编译成运行时执行的步骤列表——只编译、不起进程，
          单向依赖。
        </p>
      </article>
      <article class="card">
        <h3>Decision layer</h3>
        <p>
          一个纯粹的 proposal / policy 边界，把一次 LLM 调用变成带类型、经白名单校验的数据，
          之后才会有工具真正运行。
        </p>
      </article>
      <article class="card">
        <h3>Resume journal</h3>
        <p>
          持久化的事件日志是唯一真相来源；内存索引只是一个可重建的缓存，
          在重启后重建运行进度。
        </p>
      </article>
    </section>

    <section class="architecture">
      <h2>自上而下，全程受监督</h2>
      <p>
        每一次工具调用都跨越进程边界。一次工具崩溃对运行来说只是数据，而不是 session 的崩溃。
      </p>
      <img src="/supervision-tree.svg" alt="Soma 监督树" />
    </section>

    <section class="quickstart">
      <h2>快速开始</h2>
      <Code code={quickStart} lang="bash" />
    </section>

    <section class="why">
      <h2>为什么是 Erlang/OTP</h2>
      <div class="why-grid">
        <div>
          <h4>真正的取消</h4>
          <p>取消是一条消息，会停掉正在运行的工具进程，而不是在结尾检查一个标志位。</p>
        </div>
        <div>
          <h4>崩溃隔离</h4>
          <p>link、monitor 和监督树承担容错工作——而不是一堆防御式的 try/catch。</p>
        </div>
        <div>
          <h4>事件是强制的</h4>
          <p>从第一天起，每一个 session、run 和 tool call 都发出可持久化、可查询的事件。</p>
        </div>
      </div>
    </section>

    <footer class="landing-footer">
      <a href="https://github.com/ZhenchongLi/soma">github.com/ZhenchongLi/soma</a>
      <a href="/">English</a>
    </footer>
  </body>
</html>
