---
// 简体中文落地页，构建到 /zh/index.html。
// 独立的 Astro 页面：<html lang="zh-CN">、语言切换链接、导航都在这里手写。
// 中文是单独写的「人话」版本，不是英文的逐句翻译。
import { Code } from 'astro:components';
import '../../styles/custom.css';
import '../../styles/landing.css';

const quickStart = `# 启动后台服务，再跑一个步骤清单
soma daemon
soma run ./pipeline.lfe
soma status`;
---

<html lang="zh-CN">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="icon" href="/favicon.svg" type="image/svg+xml" />
    <title>Soma — 长在 Erlang/OTP 上的 agent 运行时</title>
    <meta
      name="description"
      content="Soma 把每一次 agent 运行做成一棵受监督的进程树，而不是一个在循环里反复调工具的函数。超时、卡死、崩溃、取消，都能稳稳接住。"
    />
  </head>
  <body>
    <nav class="landing-nav">
      <a class="brand" href="/zh/">Soma</a>
      <div class="nav-links">
        <a href="/zh/start/overview/">文档</a>
        <a href="https://github.com/ZhenchongLi/soma">GitHub</a>
        <a class="lang-toggle" href="/">EN</a>
      </div>
    </nav>

    <header class="hero">
      <h1>一次 agent 运行，是一棵受监督的 OTP 进程树。</h1>
      <p class="hero-tagline">
        Soma 是一个长在 Erlang/OTP 上的 agent 运行时。它把每一次对话、每一次运行、
        每一次工具调用，都当成一个独立、有人盯着的进程来跑——而不是一个在循环里
        反复调工具的函数。
      </p>
      <div class="hero-actions">
        <a class="cta primary" href="/zh/start/overview/">快速上手</a>
        <a class="cta" href="https://github.com/ZhenchongLi/soma">去 GitHub 看看</a>
      </div>
    </header>

    <section class="thesis">
      <p>
        想法其实很简单：<strong>agent 出问题，往往不是算错了，而是工程上出岔子</strong>——
        会超时、会卡死、会崩溃，还得能中途喊停。这些麻烦，Erlang/OTP 几十年前就处理得
        很好了：让出错的那一块安全地倒下，不连累旁边。所以 Soma 把一次运行做成一棵进程树，
        而不是一段闷头跑到底的循环。
      </p>
    </section>

    <section class="features">
      <article class="card">
        <h3>一段脚本，写清要做的事</h3>
        <p>
          用一小段 Lisp 风格的写法，说清楚「分几步、每步干什么」，再翻译成运行时照着
          执行的清单。只做翻译，不会偷偷替你跑任何东西。
        </p>
      </article>
      <article class="card">
        <h3>先想清楚，再动手</h3>
        <p>
          模型想做什么，先落成一条「提议」，过一遍白名单规则；确认放行了，工具才真的
          动手——不是模型说一句就直接执行。
        </p>
      </article>
      <article class="card">
        <h3>崩了也能接着跑</h3>
        <p>
          每一步都写进一份持久的日志，这份日志才是唯一的事实来源。就算整个程序重启，
          也能照着它把「跑到哪儿了」重新拼回来。
        </p>
      </article>
    </section>

    <section class="architecture">
      <h2>从上到下，全程有人盯着</h2>
      <p>
        每一次工具调用，都跑在自己独立的进程里。某个工具崩了，对这次运行来说只是一条
        「它挂了」的记录，不会把整场对话也拖下水。
      </p>
      <img src="/supervision-tree.svg" alt="Soma 的进程监督树" />
    </section>

    <section class="quickstart">
      <h2>快速开始</h2>
      <Code code={quickStart} lang="bash" />
    </section>

    <section class="why">
      <h2>为什么是 Erlang/OTP</h2>
      <div class="why-grid">
        <div>
          <h4>取消是真的</h4>
          <p>点了取消，是真去把正在跑的那个工具停掉——不是等它跑完，再回头看一眼你刚才想取消。</p>
        </div>
        <div>
          <h4>出错互不连累</h4>
          <p>一处崩溃就地被隔离，靠的是 Erlang 自带的监管机制，而不是到处写满 try/catch 兜底。</p>
        </div>
        <div>
          <h4>全程留痕</h4>
          <p>从第一天起，每一次对话、每一次运行、每一次工具调用，都会留下可查询、可回放的记录。</p>
        </div>
      </div>
    </section>

    <footer class="landing-footer">
      <a href="https://github.com/ZhenchongLi/soma">github.com/ZhenchongLi/soma</a>
      <a href="/">English</a>
    </footer>
  </body>
</html>
